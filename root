# cron jobs to run the scripts which update the database and make trade decisions.
# The cron jobs run every 15 minutes but effectively they only run once per day
# because those scripts check whether any subsystems require the script to fully run yet.

0,15,30,45 * * * * cd /home && /venv/bin/python -m src.runners.update_ohlc >> /home/logs/update_ohlc.log 2>&1
5,20,35,50 * * * * cd /home && /venv/bin/python -m src.runners.update_strategy >> /home/logs/update_strategy.log 2>&1
10,15,40,55 * * * * cd /home && /venv/bin/python -m src.runners.position_and_order >> /home/logs/position_and_order.log 2>&1
